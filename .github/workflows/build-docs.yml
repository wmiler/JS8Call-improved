name: Build and Deploy Docs
# Trigger only if we have labeled the PR, a Release has been published or a manual trigger
on:
  workflow_dispatch:
  pull_request:
    types: [labeled]
  release:
    types: [published]

permissions:
  contents: write
  id-token: write
  pages: write
jobs:
  build_docs:
    if: (contains(github.event.pull_request.labels.*.name, 'documentation') ||
      (contains(github.event_name, 'workflow_dispatch')) ||
      (contains(github.event_name, 'release')) )
    concurrency: ci-${{ github.ref }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get doxygen and graphviz
        run: |
          sudo apt-get update
          sudo apt-get install -y doxygen graphviz flex bison make cmake g++
          wget https://www.doxygen.nl/files/doxygen-1.16.0.src.tar.gz
          tar zxf doxygen-1.16.0.src.tar.gz
          cd doxygen-1.16.0
          mkdir build
          cd build
          cmake -G "Unix Makefiles" ..
          make
          sudo make install

      - name: Build docs
        run: (cat .github/workflows/misc/Doxyfile; echo "PROJECT_NUMBER=master";) | doxygen -

      - name: Upload static files as artifact
        id: deployment
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/html

# Only run deploy if we are on master branch
  deploy:
    if: github.ref == 'refs/heads/master'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build_docs
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
